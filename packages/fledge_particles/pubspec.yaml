name: fledge_particles
description: CPU-driven particle system for Fledge — emitters, particle pooling, presets, and DrawLayer.particles integration.
version: 0.2.0
repository: https://github.com/fledge-framework/fledge/tree/main/packages/fledge_particles
homepage: https://fledge-framework.dev
issue_tracker: https://github.com/fledge-framework/fledge/issues

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  fledge_ecs: ^0.2.1
  fledge_render_2d: ^0.2.1
  fledge_assets: ^0.2.1
  vector_math: ^2.2.0
  meta: ^1.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0

topics:
  - ecs
  - game-engine
  - particles
  - flutter-games
