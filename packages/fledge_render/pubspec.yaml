name: fledge_render
description: Deprecated - merged into fledge_render_2d. Re-export shim; import package:fledge_render_2d/fledge_render_2d.dart directly.
version: 0.2.2
repository: https://github.com/fledge-framework/fledge/tree/main/packages/fledge_render
homepage: https://fledge-framework.dev
issue_tracker: https://github.com/fledge-framework/fledge/issues

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  fledge_render_2d: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0

topics:
  - ecs
  - game-engine
  - rendering
  - game-development
